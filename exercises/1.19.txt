Summarizing prompt:

T: a <- a + b, b <- a
T^n(a=1,b=0) -> (fib(n+1),fib(n))

T = T(p=0,q=1) where T(a,b): a <- bq + aq + ap, b <- bp + aq
... probably should use a richer file type for super/subscripts/formulas.

T(p,q)^2 = T(p',q')